# ─────────────────────────────────────────────────────────────
#  requirements.txt — Semantic Similarity Search (Streamlit)
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Web framework
streamlit>=1.35.0

# Vector database (in-memory / ephemeral client)
chromadb>=0.5.0

# Embedding model backend
sentence-transformers>=3.0.0

# PyTorch CPU-only build (smaller, no GPU needed)
# Remove the --index-url line if you want CUDA support instead.
torch>=2.2.0
# If your platform pip cannot resolve torch automatically, install it first:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# Hugging Face model hub (pulled in by sentence-transformers, pinned for stability)
huggingface-hub>=0.23.0
transformers>=4.40.0
tokenizers>=0.19.0